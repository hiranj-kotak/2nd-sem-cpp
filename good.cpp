#include "bits/stdc++.h"
using namespace std;
int main()
{
    ifstream in("data100.txt");
    if (!in)
    {
        cout << "cannot oper file" << endl;
        cout << "know bit" << in.bad();
    }
    return 0;
}